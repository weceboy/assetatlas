# AssetAtlas

AI-assisted scene-to-asset atlas pipeline. MVP implementation in Python/FastAPI with modular scene parsing, asset requirement extraction, deduplication, atlas planning, segmentation hooks, metadata, and semantic search.
